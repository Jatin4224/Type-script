//type inference  in TypeScript refers to the ability of the Typescript refers to the ability of the ts compiler to automatically determine and assign
// type of variables ,expressions nad function return values based on their usage and context in the code.
// khud type likhna mtlb - type annotation

let myName: string = "jai"; //type annotation
// const myName1 = "jatin"; //type inference
// myName = 5; //it cannot be done because myName is string
myName = "jatin";
